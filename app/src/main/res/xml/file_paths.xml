<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Internal storage -->
    <files-path
        name="internal_files"
        path="." />

    <!-- External files (örneğin getExternalFilesDir) -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Eğer apkFile Downloads klasöründeyse -->
    <external-path
        name="external_storage"
        path="." />

    <!-- Eğer cache dizinindeyse -->
    <cache-path
        name="cache"
        path="." />
</paths>
